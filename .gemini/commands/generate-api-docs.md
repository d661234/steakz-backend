# Custom Command: Generate API Docs

## Instruction
When this command is invoked, follow these steps:
1. Parse all files in `src/routes/` to extract endpoints, methods, and required roles.
2. Update `API.md` with the extracted information.
3. Ensure the documentation follows a consistent format (Endpoint, Method, Auth, Roles, Description).
